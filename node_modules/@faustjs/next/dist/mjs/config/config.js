import defaults from 'lodash/defaults';
let nextConfig = {
    revalidate: 900,
};
export function config(cfg) {
    nextConfig = defaults({}, cfg, nextConfig, {
        revalidate: 900,
    });
    return nextConfig;
}
